# ToDo

- [ ] Add a minimal runtime switch to use either current local world navigation (existing proven flow) or a live telnet connection to `www.holyquest.org:7373` via `github.com/reiver/go-telnet`.
