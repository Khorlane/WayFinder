
WAYFINDER PROJECT CONTEXT
Paste this into a new ChatGPT session to continue the conversation with full context.

------------------------------------------------
PROJECT: WayFinder
Language: Go
Repository: WayFinder (local project, public GitHub repo)

Purpose:
WayFinder is a MUD navigation client and automapper. It connects to a MUD via telnet,
discovers rooms incrementally, and builds a spatial grid map based on directional exits.

The map is NOT preloaded from the world files when used with a real MUD.
Rooms are discovered dynamically as the player moves.

------------------------------------------------
HIGH LEVEL ARCHITECTURE

WayFinder
│
├── WayFinder Client Shell (WCS)
│      - application window host
│      - command input
│      - MUD output display
│      - map panel host (WMP)
│      - event routing
│
├── WayFinder Event Gateway (WEG)
│      - MUD text parsing
│      - normalized event emission
│
├── WayFinder Navigation Engine (WNE)
│      - navigation session
│      - discovery tracking
│      - topology events
│
├── WayFinder Mapping Runtime (WMR)
│      - incremental room placement
│      - spatial constraints
│      - rebuild logic
│      - layout projection
│
└── WayFinder Map Panel (WMP)
       - hosted inside WCS
       - displays map data produced by WMR

Runtime flow:
WCS → WEG → WNE → WMR

Important:
WMP is a UI panel owned by WCS, not a separate pipeline stage.

------------------------------------------------
CURRENT IMPLEMENTATION STATUS

Core Mapping Engine:
DONE

Features implemented:
- incremental map construction
- discovery model (rooms only appear when discovered)
- ordered alignment constraints
- lane reservation (no rooms inserted between locked pairs)
- correction engine
- makeRoom algorithm
- multi-step planner fallback
- rebuild fallback
- topology constraints preserved

Solver Architecture:
DONE

Structure:

Mapper
   ↓
SolverEngine interface
   ↓
ConstraintSolver implementation

Solver extracted to its own package:

solver/
    solver.go

Solver API:
- ValidateConstraintSet()
- ComputeRebuildResult()

Mapper converts internal structures into solver structures.

Recent completed refactors:
- WF-003 completed: `main.go` reduced to bootstrap/composition entrypoint (commit: `a5f7625`)
- WF-004 completed: moved subsystem code to top-level `wcs/`, `wne/`, `wmr/`, `solver/` layout (commit: `f616a65`)
- WCS Win32 shell remains available from CLI via `gui` command

------------------------------------------------
CURRENT PROJECT STRUCTURE

Project root:

WayFinder/
│
├── main.go
│
├── wcs/
│   └── win32/
│       ├── shell_windows.go
│       └── proc_windows.go
│
├── wne/
│      navigation_session.go
│      navigation_session_test.go
│
├── wmr/
│      runtime.go
│
├── solver/
│      solver.go
│
├── docs/
│      architecture.md
│      mapper_rules.md
│      discovery_model.md
│      dev/llm_context.txt
│      dev/chat_context.txt
│
├── go.mod
├── README.md
├── LICENSE
└── .gitignore

------------------------------------------------
KEY DESIGN RULES

Discovery Model:
- rooms are discovered as entered
- exits revealed immediately when room discovered
- no hidden exits exist in this world

Spatial Constraints:
- N/S relationships require same column
- E/W relationships require same row
- ordering must be preserved
- gaps allowed between rooms
- no room allowed between locked adjacency pairs

Example:
D3B above ECE

Valid:

D3B
ECE

D3B
.
ECE

But not:

D3B
NEWROOM
ECE

------------------------------------------------
MAP LAYOUT MODEL

Coordinates are an infinite integer grid.

Rooms contain:

RoomID
Placed flag
Row
Column

Occupancy map tracks which room occupies each cell.

Locks represent discovered adjacencies that must remain valid.

------------------------------------------------
REBUILD SYSTEM

When incremental placement cannot resolve conflicts,
the solver performs a rebuild of the discovered map.

Rebuild process:
1. build constraint set
2. run solver
3. produce new room layout
4. mapper applies results

------------------------------------------------
SOLVER PACKAGE

solver/
    solver.go

Major types:

SolverContext
SolverRoomState
ConstraintSet
ConstraintRelation
LockedAdjKey
RebuildResult
RebuildRoomState

SolverEngine interface:

ValidateConstraintSet()
ComputeRebuildResult()

Current implementation:

ConstraintSolver

------------------------------------------------
FUTURE WORK

1. Telnet integration
library:
github.com/reiver/go-telnet

2. WCS integration upgrades (Win32)
- route real command input/output through WCS panels
- connect WCS ports to WEG/WNE/WMR
- keep WCS as a thin shell (no parsing/navigation/topology logic)

UI layout target (already stubbed):

+----------------------+---------------+
| MUD output window    | Map panel     |
|                      |               |
|                      |               |
+----------------------+---------------+
| Command input field                  |
+--------------------------------------+

3. Renderer
Replace placeholder map panel text with WMR-driven map drawing in WMP (no Fyne dependency).

------------------------------------------------
NAMING CONVENTIONS

WayFinder Client Shell (WCS)
WayFinder Event Gateway (WEG)
WayFinder Navigation Engine (WNE)
WayFinder Mapping Runtime (WMR)
WayFinder Map Panel (WMP)

------------------------------------------------
CURRENT DEVELOPMENT WORKFLOW

1. ChatGPT designs architecture steps
2. Instructions given to Codex (VSCode extension)
3. Codex edits files and ensures build success
4. Updated files reviewed
5. Next task generated

Canonical task tracker: docs/dev/ToDo.md
Canonical Go source inventory: docs/dev/go_source_index.md
Refresh rule: regenerate `docs/dev/go_source_index.md` after any Go file add/remove/rename or major signature changes.
Regeneration steps for `go_source_index.md`: docs/dev/MAINTENANCE.md
Generator implementation: docs/dev/scripts/generate_go_source_index.go (AST-based; includes package-level variables).

------------------------------------------------
CURRENT STATUS SUMMARY

Mapping engine: complete
Solver architecture: complete
Package separation: major structure complete (`wcs/`, `wne/`, `wmr/`, `solver/`)
Telnet integration: not started
GUI: started (Win32 WCS stub available via `gui` command)

Estimated completion: ~75-80%

Top open TODOs:
- WF-001 minimal runtime switch: local world navigation vs live telnet (`www.holyquest.org:7373`)
- WF-002 remove temporary `Rooms/` folder when no longer needed

Remaining work is mostly WCS/WEG integration, telnet integration, and map panel rendering.

------------------------------------------------
